alias g="git"
alias gcaa="git add --all && git commit --amend --no-edit"
alias gdc="git diff --cached"
alias gdcs="git diff --cached --shortstat"
alias gds="git diff --shortstat"
alias t="tmux -u -2"
alias e="emacsclient"
alias ta="tig --all"
alias gc="git commit"
alias sb="cd /Users/pierre/code/bardeen/bardeen-client/packages/bardeen-design && yarn storybook"
alias vim="nvim"

# Intuitive map function
# For example, to list all directories that contain a certain file:
# find . -name .gitattributes | map dirname
alias map="xargs -n1"

alias fixup="git commit -n --fixup=\`rev\`"
alias gogo="g aa && gcan! -n && g pf"
